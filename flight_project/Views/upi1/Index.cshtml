@model IEnumerable<flight_project.Models.upi1>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UPI_PIN)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UPI_PIN)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UPI_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.UPI_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UPI_Id })
        </td>
    </tr>
}

</table>
